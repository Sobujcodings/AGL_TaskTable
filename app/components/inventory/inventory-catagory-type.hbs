<Inventory::InventoryTable 
@columns={{this.inventoryTypeColumns}} @items={{this.items}}
@handleCancel={{this.handleCancel}} @handleEdit={{this.handleEdit}} @handleSave={{this.handleSave}}
@tabledropdown={{this.TableDropDownnames}} @handleStatus={{this.handleStatus}} @selectedValue={{this.name}}
/>
