import Component from '@glimmer/component';
import { action } from '@ember/object';
import { tracked } from '@glimmer/tracking';

export default class LibraryComponent extends Component {
    // @tracked activeTab = '';
    // // @tracked inactive = false;

    // // click hole table title ta change kore dibo
    // @action
    // switchTab(tabname,id) {
    //     console.log(tabname,id);
    //     // console.log('click in library.js', this.activeTab);
    //     this.activeTab = tabname;
    //     // console.log(this.activeTab);
    // }
}
